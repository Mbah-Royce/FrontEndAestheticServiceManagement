package com.se3.ase.ui.login;

import androidx.annotation.NonNull;
import androidx.lifecycle.ViewModel;
import androidx.lifecycle.ViewModelProvider;

import com.se3.ase.data.AuthDataSource;
import com.se3.ase.data.LoginRepository;

public class LoginViewModelFactory {
//implements ViewModelProvider.Factory
//    @NonNull
//    @Override
//    @SuppressWarnings("unchecked")
//    public <T extends ViewModel> T create(@NonNull Class<T> modelClass) {
//        if (modelClass.isAssignableFrom(LoginViewModel.class)) {
//            return (T) new LoginViewModel(LoginRepository.getInstance(new AuthDataSource()));
//        } else {
//            throw new IllegalArgumentException("Unknown ViewModel class");
//        }
//    }
}
